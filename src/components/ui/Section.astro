---
const { heading } = Astro.props
---

<section class="px-8 py-4" aria-labelledby={heading}>
  <header class="max-w-3xl mb-4 space-y-2 leading-relaxed">
    <h1
      id={heading}
      class="font-semibold leading-tight tracking-tight text-balance"
    >
      <slot name="title" />
    </h1>
    <p class="md:text-xl text-pretty text-copper-100">
      <slot name="description" />
    </p>
  </header>
  <slot />
</section>
