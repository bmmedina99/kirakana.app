---
const { name, width = 24, height = 24, class: classes } = Astro.props
---

<svg
  width={width}
  height={height}
  viewBox="0 0 24 24"
  fill='none'
  stroke="currentColor"
  stroke-width="2"
  class:list={[{ [classes]: classes }]}
>
  <use href={`/svg/sprite.svg#${name}`} />
</svg>
